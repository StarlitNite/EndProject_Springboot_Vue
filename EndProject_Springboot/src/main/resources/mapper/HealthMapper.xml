<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.endproject.dao.HealthClockDao">

    <select id="getHealthByStu" resultType="com.endproject.entity.HealthClock">
        select * from health_clock where snum = #{snum} order by health_clock.snum asc limit ${Limit} offset ${Page};
    </select>

    <select id="getLeaveByCou" resultType="com.endproject.entity.HealthClock">
        SELECT h.id,h.snum,h.local,h.confirmed,h.asymptomatic,h.today_temp,h.fever_and_cough,h.cough,h.recent_home,h.create_time FROM health_clock as h
        LEFT JOIN `user` ON `user`.snum = h.snum WHERE `user`.counselor_id=
        (SELECT counselor.id FROM counselor LEFT JOIN `user` ON `user`.snum = counselor.cnum WHERE snum=#{snum})
        order by h.snum asc limit ${Limit} offset ${Page};
    </select>
    <select id="check" resultType="java.lang.Boolean">
        SELECT count(*) FROM health_clock WHERE TO_DAYS(#{create_time}) = TO_DAYS(NOW()) AND snum = #{snum}
    </select>


</mapper>